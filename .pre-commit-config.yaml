# Installation des types de hooks nécessaires lors de `pre-commit install`
# (sans cette directive, le hook commit-msg ci-dessous ne serait pas posé).
default_install_hook_types: [pre-commit, commit-msg]

# NOTE : validations temporairement suspendues.
# Toutes les vérifications sont commentées ci-dessous ; `repos: []` garde le
# fichier valide pour pre-commit. Décommenter pour réactiver les contrôles.
repos: []
#repos:
#  - repo: https://github.com/pre-commit/pre-commit-hooks
#    rev: v5.0.0
#    hooks:
#      - id: trailing-whitespace
#      - id: end-of-file-fixer
#      - id: check-yaml
#      - id: check-toml
#      - id: check-added-large-files
#        args: ["--maxkb=1000"]
#      - id: debug-statements
#
#  - repo: https://github.com/astral-sh/ruff-pre-commit
#    rev: v0.11.2
#    hooks:
#      - id: ruff
#        args: [--fix]
#      - id: ruff-format
#
#  - repo: https://github.com/pre-commit/mirrors-mypy
#    rev: v1.15.0
#    hooks:
#      - id: mypy
#        additional_dependencies:
#          - duckdb>=1.5.1
#          - narwhals>=1.48.1
#          - polars>=1.39.3
#          - pyarrow>=23.0.1
#
#  # Validation du format Conventional Commits sur le message de commit.
#  # Indispensable au changelog : release-please n'incrémente la version et
#  # ne génère les notes qu'à partir de commits conventionnels.
#  - repo: https://github.com/compilerla/conventional-pre-commit
#    rev: v3.6.0
#    hooks:
#      - id: conventional-pre-commit
#        stages: [commit-msg]
